# Environment variables for Docker Compose
# Copy this file to .env and update with your actual values

# GitHub OAuth Configuration
GITHUB_CLIENT_ID=Ov23li0Ktpp1f9YEvsG6
GITHUB_CLIENT_SECRET=your_actual_client_secret_here

# MongoDB Configuration (optional - defaults are set in docker-compose.yml)
MONGODB_ROOT_USERNAME=admin
MONGODB_ROOT_PASSWORD=password123
MONGODB_DATABASE=todolist

# RabbitMQ Configuration (optional - defaults are set in docker-compose.yml)
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest

# Application Configuration
SERVER_PORT=8080
LOG_LEVEL=DEBUG
